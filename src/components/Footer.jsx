import React from "react";
import classes from "../components/Footer.module.css";

const Footer = () => {
  return <footer className={classes.footer}>Footer will be here</footer>;
};

export default Footer;
